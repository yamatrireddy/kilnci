{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The only capability: the main window may call Kiln's five commands. No filesystem, shell, opener, HTTP, or other core permissions are granted to the webview (security-standards §10, T-35).",
  "windows": ["main"],
  "permissions": [
    "allow-get-server-url",
    "allow-set-server-url",
    "allow-sign-in",
    "allow-sign-out",
    "allow-api-request"
  ]
}
